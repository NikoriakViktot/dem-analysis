# Base directory where all data files (GeoTIFF, Parquet, CSV, etc.) are stored.
# Replace with your absolute or relative path (e.g., /home/user/data or ./data)
DATA_PATH=/absolute/path/to/your/data

# (optional) Expected coordinate reference system for spatial data
EXPECTED_CRS=EPSG:4326

# (optional) Minimum acceptable DEM resolution in meters (used for validation)
MIN_RESOLUTION=30

# (optional) Path to ICESat-2 ground truth file (ATL08 filtered points)
ICESAT2_REF_PATH=/path/to/icesat2_ground_points.parquet
